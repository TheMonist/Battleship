const gameBoard = require('../gameboard');
const ship = require('../ship');

let board1 = Gameboard(1);

test('', () => {
    expect()
});

test('', () => {
    expect()
});

test('', () => {
    expect()
});

test('', () => {
    expect()
});